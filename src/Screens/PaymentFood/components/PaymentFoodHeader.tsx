import React from "react";
import { StyleSheet, Text, View } from "react-native";

type PaymentFoodHeaderProps = {
  title: string;
};

export const PaymentFoodHeader = ({ title }: PaymentFoodHeaderProps) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};
